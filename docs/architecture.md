# 🏗️ Application Architecture

## Overview

This document describes the architectural patterns, design decisions, and system organization of the E-Storefront application.

---

## 🎯 Architecture Principles

### Core Principles

| Principle | Description |
|-----------|-------------|
| **Separation of Concerns** | Clear boundaries between layers |
| **Single Responsibility** | Each module has one purpose |
| **DRY (Don't Repeat Yourself)** | Reusable components and utilities |
| **KISS (Keep It Simple)** | Simplest solution that works |
| **Scalability** | Design for growth |

---

## 📊 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              CLIENT BROWSER                                  │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                         React Application                            │    │
│  │  ┌──────────────────────────────────────────────────────────────┐   │    │
│  │  │                    PRESENTATION LAYER                         │   │    │
│  │  │   Pages (app/) │ Components │ Layouts │ UI Library            │   │    │
│  │  └──────────────────────────────────────────────────────────────┘   │    │
│  │                               │                                      │    │
│  │  ┌──────────────────────────────────────────────────────────────┐   │    │
│  │  │                      STATE LAYER                              │   │    │
│  │  │   Zustand (cart, user) │ Recoil (filters) │ React Query       │   │    │
│  │  └──────────────────────────────────────────────────────────────┘   │    │
│  │                               │                                      │    │
│  │  ┌──────────────────────────────────────────────────────────────┐   │    │
│  │  │                       DATA LAYER                              │   │    │
│  │  │   Custom Hooks │ Apollo Client │ API Utilities                │   │    │
│  │  └──────────────────────────────────────────────────────────────┘   │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      │ HTTPS
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                             NEXT.JS SERVER                                   │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │  Server Components │ API Routes │ Middleware │ Edge Functions       │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                    ┌─────────────────┼─────────────────┐
                    ▼                 ▼                 ▼
          ┌─────────────────┐ ┌─────────────┐ ┌─────────────────┐
          │   GraphQL API   │ │  Auth API   │ │      CDN        │
          │  ─────────────  │ │ ─────────── │ │ ─────────────── │
          │  Products       │ │  JWT Auth   │ │  Static Assets  │
          │  Orders         │ │  OAuth      │ │  Images         │
          │  Categories     │ │  Sessions   │ │  Fonts          │
          │  Reviews        │ │             │ │                 │
          └─────────────────┘ └─────────────┘ └─────────────────┘
```

---

## 📁 Layer Architecture

### 1. Presentation Layer

**Purpose:** Render UI and handle user interactions

```
app/                          # Pages and layouts
├── layout.tsx                # Root layout (providers, header, footer)
├── page.tsx                  # Homepage
├── products/                 # Product pages
│   ├── page.tsx             # Product listing
│   ├── layout.tsx           # Product layout
│   └── [id]/                # Dynamic product detail
│       └── page.tsx
└── ...

components/                   # Reusable UI components
├── Header.tsx               # Navigation header
├── Footer.tsx               # Site footer
├── ProductCard.tsx          # Product display card
├── ProductSlider.tsx        # Carousel component
└── ...
```

**Key Patterns:**
- **Server Components** - Default for data fetching
- **Client Components** - For interactivity (`"use client"`)
- **Composition** - Small, composable components
- **Colocation** - Related files together

---

### 2. State Layer

**Purpose:** Manage application state across components

```
store/                        # State management
├── cartStore.ts             # Zustand - Cart state
├── categoryStore.ts         # Zustand - Category cache
├── recoilState.ts           # Recoil - Filters, search
└── index.ts                 # Export aggregation
```

**State Categories:**

| Type | Technology | Examples |
|------|------------|----------|
| **Client State** | Zustand | Cart, wishlist, user preferences |
| **UI State** | Recoil | Filters, sort order, search query |
| **Server State** | React Query | Products, orders, reviews |
| **URL State** | Next.js Router | Page, product ID, filter params |

**State Flow:**

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   Action    │────▶│   Store     │────▶│ Component   │
│ (user click)│     │ (Zustand)   │     │ (re-render) │
└─────────────┘     └─────────────┘     └─────────────┘
                           │
                           ▼
                    ┌─────────────┐
                    │ Persistence │
                    │(localStorage)│
                    └─────────────┘
```

---

### 3. Data Layer

**Purpose:** Handle data fetching, caching, and API communication

```
lib/                          # Data layer
├── apollo/                   # GraphQL client
│   ├── client.ts            # Apollo configuration
│   └── queries/             # GraphQL operations
│       ├── index.ts         # All queries/mutations
│       └── fragments.ts     # Shared fragments
└── hooks/                    # Data fetching hooks
    ├── useProducts.ts       # Product operations
    ├── useOrders.ts         # Order operations
    ├── useAuth.ts           # Authentication
    └── ...
```

**Data Flow:**

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  Component  │────▶│   Hook      │────▶│ Apollo/Query│────▶│   GraphQL   │
│             │     │ (useProducts)│     │  Client     │     │    API      │
└─────────────┘     └─────────────┘     └─────────────┘     └─────────────┘
                           │
                           ▼
                    ┌─────────────┐
                    │   Cache     │
                    │(React Query)│
                    └─────────────┘
```

---

## 🔄 Data Flow Patterns

### Read Pattern (Product Listing)

```
1. User navigates to /products
         │
         ▼
2. Server Component renders
         │
         ▼
3. useProducts hook called
         │
         ▼
4. React Query checks cache
         │
    ┌────┴────┐
    ▼         ▼
 CACHED    STALE/MISS
    │         │
    │         ▼
    │    5. Apollo fetches
    │         │
    │         ▼
    │    6. Cache updated
    │         │
    └────┬────┘
         ▼
7. Component renders with data
```

### Write Pattern (Add to Cart)

```
1. User clicks "Add to Cart"
         │
         ▼
2. addItem action dispatched
         │
         ▼
3. Zustand store updates
         │
    ┌────┴────┐
    ▼         ▼
 State      Persist
Updated    to localStorage
    │         │
    └────┬────┘
         ▼
4. Subscribed components re-render
         │
         ▼
5. Cart count updates in header
```

---

## 🧩 Component Architecture

### Component Hierarchy

```
RootLayout (app/layout.tsx)
├── Providers (QueryClient, Apollo, Recoil, Zustand)
│   ├── Header
│   │   ├── Logo
│   │   ├── SearchBar
│   │   ├── Navigation
│   │   └── CartIcon (badge with count)
│   ├── {children} (Page content)
│   │   └── [Page-specific components]
│   └── Footer
│       ├── Links
│       ├── Newsletter
│       └── Copyright
```

### Component Types

| Type | Location | Purpose | Example |
|------|----------|---------|---------|
| **Pages** | `app/*/page.tsx` | Route handlers | ProductsPage |
| **Layouts** | `app/*/layout.tsx` | Shared structure | ProductsLayout |
| **Feature** | `components/` | Business logic | ProductCard |
| **UI** | `@3asoftwares/ui` | Generic UI | Button, Modal |

### Component Communication

```
┌─────────────────────────────────────────────────────────────┐
│                        Page Component                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │   Header    │  │   Content   │  │   Footer    │          │
│  │ ─────────── │  │ ─────────── │  │ ─────────── │          │
│  │ Props: user │  │ Props: data │  │ Props: none │          │
│  │ Events: nav │  │ Events: act │  │ Events: nav │          │
│  └─────────────┘  └─────────────┘  └─────────────┘          │
│         │                │                                   │
│         └───────┬────────┘                                   │
│                 ▼                                            │
│         ┌─────────────┐                                      │
│         │ Zustand/    │    Global state for cross-component  │
│         │ Recoil      │    communication                     │
│         └─────────────┘                                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔐 Security Architecture

### Authentication Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         AUTHENTICATION FLOW                              │
└─────────────────────────────────────────────────────────────────────────┘

1. LOGIN REQUEST
   ┌──────────┐    Credentials    ┌──────────┐    Verify    ┌──────────┐
   │  Client  │─────────────────▶│ Auth API │────────────▶│ Database │
   └──────────┘                   └──────────┘              └──────────┘
        │                              │
        │◀──────── JWT Tokens ─────────┘
        │
2. STORE TOKENS
        │
        ▼
   ┌──────────────────────────────┐
   │   Cookie Storage (HttpOnly)  │
   │   ├── Access Token (15min)   │
   │   └── Refresh Token (7days)  │
   └──────────────────────────────┘

3. AUTHENTICATED REQUEST
   ┌──────────┐   + Bearer Token   ┌──────────┐
   │  Client  │──────────────────▶│ GraphQL  │
   │(Apollo)  │                    │   API    │
   └──────────┘                    └──────────┘

4. TOKEN REFRESH (automatic)
   ┌──────────┐  Expired Token    ┌──────────┐
   │  Apollo  │─────────────────▶│ Auth API │
   │  Link    │                   └──────────┘
   └──────────┘                        │
        │◀──────── New Tokens ─────────┘
```

### Authorization Layers

| Layer | Responsibility |
|-------|---------------|
| **Client** | UI-level access control (show/hide) |
| **API Route** | Request validation |
| **GraphQL** | Query/mutation authorization |
| **Database** | Row-level security |

---

## 🚀 Performance Architecture

### Rendering Strategy

| Page Type | Strategy | Reason |
|-----------|----------|--------|
| **Homepage** | SSR + ISR | Fresh data, fast subsequent loads |
| **Product Listing** | SSR | SEO, dynamic filters |
| **Product Detail** | SSG + ISR | SEO, mostly static content |
| **Cart/Checkout** | CSR | Private, interactive |
| **Profile** | CSR | Private, user-specific |

### Caching Strategy

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           CACHING LAYERS                                 │
└─────────────────────────────────────────────────────────────────────────┘

1. BROWSER CACHE
   ├── Static assets (JS, CSS, images)
   └── Service Worker (PWA)

2. CDN CACHE (Vercel Edge)
   ├── Static pages
   └── Revalidated pages

3. APPLICATION CACHE
   ├── React Query (server state)
   ├── Apollo InMemoryCache (GraphQL)
   └── Zustand persist (client state)

4. API CACHE
   └── GraphQL response caching
```

---

## 📦 Module Architecture

### Shared Packages

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        PACKAGE ECOSYSTEM                                 │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────┐   ┌─────────────────┐   ┌─────────────────┐
│ @3asoftwares/ui │   │@3asoftwares/    │   │@3asoftwares/    │
│                 │   │     types       │   │     utils       │
│ ─────────────── │   │ ─────────────── │   │ ─────────────── │
│ Button          │   │ Product         │   │ formatPrice     │
│ Card            │   │ Order           │   │ parseDate       │
│ Modal           │   │ User            │   │ auth helpers    │
│ Input           │   │ Cart            │   │ validation      │
└────────┬────────┘   └────────┬────────┘   └────────┬────────┘
         │                     │                     │
         └─────────────────────┼─────────────────────┘
                               ▼
                    ┌─────────────────────┐
                    │  E-Storefront-Web   │
                    │  (This Application) │
                    └─────────────────────┘
```

### Import Structure

```typescript
// External packages
import { Button, Card } from '@3asoftwares/ui';
import { Product, Order } from '@3asoftwares/types';
import { formatPrice, parseDate } from '@3asoftwares/utils';

// Internal modules
import { useProducts } from '@/lib/hooks/useProducts';
import { useCartStore } from '@/store/cartStore';
import { ProductCard } from '@/components/ProductCard';
```

---

## 🔧 Configuration Architecture

### Environment Configuration

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      ENVIRONMENT CONFIGURATION                           │
└─────────────────────────────────────────────────────────────────────────┘

                    ┌─────────────────────┐
                    │    .env.example     │ ← Template (committed)
                    └─────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│  .env.local     │ │ .env.staging    │ │.env.production  │
│ (Development)   │ │   (Staging)     │ │  (Production)   │
│                 │ │                 │ │                 │
│ API: localhost  │ │ API: staging    │ │ API: prod       │
│ Debug: true     │ │ Debug: true     │ │ Debug: false    │
└─────────────────┘ └─────────────────┘ └─────────────────┘
```

### Build Configuration

| File | Purpose |
|------|---------|
| `next.config.ts` | Next.js configuration |
| `tailwind.config.ts` | Tailwind CSS configuration |
| `tsconfig.json` | TypeScript configuration |
| `jest.config.js` | Testing configuration |
| `postcss.config.js` | CSS processing |

---

## 📚 Related Documentation

- [Project Structure](project-structure.md)
- [State Management](state-management.md)
- [API Layer](api-layer.md)
- [Performance](performance.md)
